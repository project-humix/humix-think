

var humix_settings = module.exports = {

    // 'local' or 'bluemix'
    location : 'bluemix',

    // 'couch' 
    storage : 'couch',

    // storageURL is needed only when deployed on local. 
    storageURL : 'http://127.0.0.1:5984/'
};
